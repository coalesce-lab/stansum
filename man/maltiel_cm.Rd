% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maltiel_cm.R
\name{maltiel_cm}
\alias{maltiel_cm}
\alias{maltiel_cm_count}
\title{Maltiel et al (2015) Combined Model}
\usage{
maltiel_cm_count(N, K, y, m, L, eta, v, ...)
}
\arguments{
\item{N}{integer; population size}

\item{K}{integer; number of sub-populations}

\item{y}{numeric matrix; ARD counts (N x K)}

\item{m}{numeric vector; fractional sub-population sizes (length K)}

\item{L}{numeric vector; lower bounds on individual degrees (length N).
Must satisfy \code{L[i] >= max(y[i,])} for each respondent \code{i} so that the
degree is always at least as large as the maximum observed count.}

\item{eta}{numeric vector; Beta prior shape 1 for each transmission bias
(length K)}

\item{v}{numeric vector; Beta prior shape 2 for each transmission bias
(length K)}

\item{...}{other arguments passed to \code{\link[cmdstanr:sample]{cmdstanr::sample()}}}
}
\value{
Object of class \link[cmdstanr:CmdStanMCMC]{cmdstanr::CmdStanMCMC} containing the samples.
}
\description{
Bayesian model for ARD with both barrier effect and
transmission bias parameters.

\itemize{
\item \code{maltiel_cm_count} -- combined model for count responses, as
described in section 2.4 of Maltiel et al (2015). It carries both the
per-sub-population overdispersion \code{rho_k} of the barrier effects model and
the transmission bias \code{tau_k} of the transmission bias model:
\code{y_ik ~ Binomial(d_i, tau_k * q_ik)} with \code{d_i ~ LogNormal(mu, sigma^2)}
and \code{q_ik ~ Beta(m_k, rho_k)}. Multiplying \code{q_ik} by \code{tau_k} breaks the
Beta-Binomial conjugacy, so -- unlike \code{\link[=maltiel_bem_count]{maltiel_bem_count()}} -- the latent
\code{q_ik} cannot be integrated out and are sampled together with the
remaining parameters. This adds \code{N * K} parameters, so fits are
substantially slower than for the component models.
}
}
\details{
Calling this function(s) will load the model object using
\code{\link[=get_model]{get_model()}} and sample from the posterior using the \code{\link[cmdstanr:sample]{cmdstanr::sample()}}
method.

The transmission biases \code{tau_k} and the degrees \code{d_i} are only
weakly identified from ARD alone: scaling \code{tau_k} down and the degrees up
leaves the reported counts largely unchanged. The \code{eta} / \code{v} prior on
\code{tau} is what resolves this, so an uninformative \code{eta = v = 1} will
typically pull the posterior for \code{tau} below 1 and inflate the estimated
degrees. Supply an informative prior reflecting what is known about
transmission for each sub-population.
}
\examples{
data("Fake_maltiel_RD", package = "stansum")
y <- data.matrix(Fake_maltiel_RD)[1:50, ]
K <- ncol(y)
r <- maltiel_cm_count(
  N = nrow(y),
  K = K,
  y = y,
  m = rep(0.01, K),
  L = apply(y, 1, max),
  eta = rep(1, K),
  v = rep(1, K),
  chains = 1,
  iter_warmup = 200,
  iter_sampling = 10
)

}
\references{
Maltiel, R., Raftery, A. E., McCormick, T. H., & Baraff, A.
J. (2015). Estimating Population Size Using the Network Scale Up
Method. \emph{The Annals of Applied Statistics}, 9(3), 1247–1277.
\doi{10.1214/15-AOAS827}
}
\seealso{
Use \code{\link[=get_model]{get_model()}} to return Stan model object of class
\link[cmdstanr:CmdStanModel]{cmdstanr::CmdStanModel} without fitting it.

Other models in this package:
\code{\link{maltiel_bem}},
\code{\link{maltiel_rdm}},
\code{\link{maltiel_tbm}},
\code{\link[=test_model]{test_model()}},
\code{\link{zheng_bem}},
\code{\link{zheng_gp}}

Other models of Maltiel et al (2015):
\code{\link{maltiel_bem}},
\code{\link{maltiel_rdm}},
\code{\link{maltiel_tbm}}
}
\concept{models in this package}
\concept{models of Maltiel et al (2015)}
